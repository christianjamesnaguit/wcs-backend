# MongoDB Atlas Connection String
# Replace <db_username> and <db_password> with your Atlas credentials
MONGODB_URI=mongodb+srv://ceijin:zeddlyte03@domoreplanner.xapwilu.mongodb.net/?appName=domoreplanner

# JWT Secret Key - use a strong random string in production
JWT_SECRET=your_strong_jwt_secret_key_here

# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend Path (for serving built Vue app)
# For development, this serves from frontend/dist after building
FRONTEND_PATH=../frontend/dist

# Netlify URL (optional - for CORS)
# NETLIFY_URL=https://your-app.netlify.app

# Railway-specific (optional - Railway auto-generates PORT)
# RAILWAY_STATIC_URL=https://your-backend-production.up.railway.app
